module concepts {
	
	/* question2:
	 * 
	 * liste: taille dynamique, taille donnée par une methode, pile potentiellement infinie,
	 * methode d'ajout en fin
	 * 
	 * tableau statique: taille definie, attribut taille, pile bornée, necessite un index qui reference
	 * le dernier element
	 */
}